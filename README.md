# Cpu-Guardian
Real-time CPU side-channel attack detection using PMU counters. C core with z-score anomaly detection + optional Python ML (Isolation Forest + One-Class SVM). Defensive only — no exploits.
